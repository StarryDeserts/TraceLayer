{
  "name": "@tracelayer/live-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc",
    "dev": "cross-env PORT=3002 node --import tsx src/server.ts"
  },
  "dependencies": {
    "@tracelayer/api": "workspace:*",
    "@tracelayer/config": "workspace:*",
    "@tracelayer/sui-anchor": "workspace:*",
    "@tracelayer/walrus": "workspace:*"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "tsx": "^4.20.6"
  }
}
